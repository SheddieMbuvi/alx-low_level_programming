#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 *@sum: the total value of the numbers
 * Return:  Always return 0
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
